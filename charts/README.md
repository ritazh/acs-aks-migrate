# acs-aks-migrate-sample

[acs-aks-migrate-sample](https://github.com/ritazh/acs-aks-migrate) is a sample app for acs-aks-migrate

## TL;DR:

```console
$ helm install . -f values.yaml
```
Where `values.yaml` contains:

```

```